include: analysis_options_strict.yaml

linter:
  rules:
    # Useful in tests and examples.
    avoid_redundant_argument_values: false

    # Useful in lib/, less useful elsewhere
    unnecessary_raw_strings: false
    