/// A write-only interface representing a terminal screen.
///
/// A screen supports basic terminal controls beyond just writing text, such as
/// clearing all or parts of the screen, moving the cursor, and changing the
/// text color or style.
abstract interface class Screen {}
