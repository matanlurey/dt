include: ../analysis_options.yaml

analyzer:
  errors:
    # OK for sample code.
    avoid_print: ignore

    # Obfuscates sample code where consistency is less important.
    cascade_invocations: ignore

    # Used to make the examples clearer.
    prefer_asserts_with_message: ignore
